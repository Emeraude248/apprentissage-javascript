let paragraphes = document.querySelector(".paragraphe")
//querySelector pour un seul élément, querySelectorAll pour plusieurs
//utilise sélector CSS3

console.log(paragraphes);
alert(paragraphes.innerHTML);
//1 résultat, pas tableau, pas for